# WebZaLokal

WebZaLokal landing page prepared for automatic deployment with Cloudflare Pages.

## Cloudflare Pages settings

- Framework preset: Next.js (Static HTML Export)
- Build command: `npm run build`
- Build output directory: `out`
- Root directory: `/`

The contact form sends enquiries to `webzalokal@gmail.com` through FormSubmit.
